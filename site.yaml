# Site configuration. Read once per build; missing keys fall back to the
# defaults in build.py (DEFAULT_CONFIG). Delete this file to use defaults
# for everything.

title: "Math for the People"
tagline: "It's math for you."
url: "https://mathforthepeople.com"  # update after pointing your domain
base_url: "/"   # set to "/mathforthepeople/" when deploying without the custom domain
description: "A stripped-down blog about mathematics."

# Build behavior — uncomment to override the defaults.
# max_rabbit_hole_depth: 3
# markdown_extensions:
#   - fenced_code
#   - tables
#   - smarty
#   - footnotes
#   - toc
#   - attr_list
